<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

    <!-- ============================================ -->
    <!-- API EXPERIENCE LAYER - BEST PRACTICES        -->
    <!-- ============================================ -->

    <!-- Flow Principal: API Experience - Health Check -->
    <flow name="api-health-check-flow" doc:name="API Health Check Flow">
        <http:listener config-ref="HTTP_Listener_config" path="${api.basePath}/health" doc:name="GET /health">
            <http:response statusCode="#[vars.httpStatus default 200]">
                <http:headers>#[output application/java --- { "X-Correlation-ID": vars.correlationId }]</http:headers>
            </http:response>
            <http:error-response statusCode="#[vars.httpStatus default 500]">
                <http:body>#[payload]</http:body>
                <http:headers>#[output application/java --- { "X-Correlation-ID": vars.correlationId }]</http:headers>
            </http:error-response>
        </http:listener>
        
        <flow-ref name="save-start-time-subflow" doc:name="Save Start Time"/>
        <flow-ref name="generate-correlation-id-subflow" doc:name="Generate Correlation ID"/>
        <flow-ref name="log-request-start-subflow" doc:name="Log Request Start"/>
        
        <ee:transform doc:name="Health Check Response">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    status: "UP",
    timestamp: now(),
    application: {
        name: p('api.name'),
        version: p('api.version'),
        environment: p('env')
    },
    correlationId: vars.correlationId
}]]></ee:set-payload>
            </ee:message>
        </ee:transform>
        
        <flow-ref name="log-request-end-subflow" doc:name="Log Request End"/>
        
        <error-handler ref="global-api-error-handler" doc:name="Global Error Handler"/>
    </flow>

    <!-- Flow Principal: API Experience - Customers -->
    <flow name="api-get-customers-flow" doc:name="API Get Customers Flow">
        <http:listener config-ref="HTTP_Listener_config" path="${api.basePath}/customers" doc:name="GET /customers"
                      allowedMethods="GET">
            <http:response statusCode="#[vars.httpStatus default 200]">
                <http:headers>#[output application/java --- { "X-Correlation-ID": vars.correlationId }]</http:headers>
            </http:response>
            <http:error-response statusCode="#[vars.httpStatus default 500]">
                <http:body>#[payload]</http:body>
                <http:headers>#[output application/java --- { "X-Correlation-ID": vars.correlationId }]</http:headers>
            </http:error-response>
        </http:listener>
        
        <flow-ref name="save-start-time-subflow" doc:name="Save Start Time"/>
        <flow-ref name="generate-correlation-id-subflow" doc:name="Generate Correlation ID"/>
        <flow-ref name="log-request-start-subflow" doc:name="Log Request Start"/>
        
        <!-- Appel à la couche Process -->
        <flow-ref name="process-get-customers-flow" doc:name="Call Process Layer"/>
        
        <!-- Transformation pour l'expérience client -->
        <ee:transform doc:name="Transform to Experience Format">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    customers: payload map ((customer, index) -> {
        id: customer.customerId,
        fullName: customer.firstName ++ " " ++ customer.lastName,
        email: customer.emailAddress,
        phone: customer.phoneNumber,
        registrationDate: customer.createdAt
    }),
    totalCount: sizeOf(payload),
    correlationId: vars.correlationId,
    timestamp: now()
}]]></ee:set-payload>
            </ee:message>
        </ee:transform>
        
        <flow-ref name="log-request-end-subflow" doc:name="Log Request End"/>
        
        <error-handler ref="global-api-error-handler" doc:name="Global Error Handler"/>
    </flow>

    <!-- Flow Principal: API Experience - Create Customer -->
    <flow name="api-create-customer-flow" doc:name="API Create Customer Flow">
        <http:listener config-ref="HTTP_Listener_config" path="${api.basePath}/customers" doc:name="POST /customers"
                      allowedMethods="POST">
            <http:response statusCode="#[vars.httpStatus default 201]">
                <http:headers>#[output application/java --- { 
                    "X-Correlation-ID": vars.correlationId,
                    "Location": "${api.basePath}/customers/" ++ vars.customerId
                }]</http:headers>
            </http:response>
            <http:error-response statusCode="#[vars.httpStatus default 500]">
                <http:body>#[payload]</http:body>
                <http:headers>#[output application/java --- { "X-Correlation-ID": vars.correlationId }]</http:headers>
            </http:error-response>
        </http:listener>
        
        <flow-ref name="save-start-time-subflow" doc:name="Save Start Time"/>
        <flow-ref name="generate-correlation-id-subflow" doc:name="Generate Correlation ID"/>
        <flow-ref name="log-request-start-subflow" doc:name="Log Request Start"/>
        
        <!-- Validation de l'input -->
        <flow-ref name="validate-input-subflow" doc:name="Validate Input"/>
        
        <!-- Appel à la couche Process -->
        <flow-ref name="process-create-customer-flow" doc:name="Call Process Layer"/>
        
        <!-- Préparation de la réponse -->
        <set-variable variableName="httpStatus" value="201" doc:name="Set HTTP Status 201"/>
        <ee:transform doc:name="Create Response">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    message: "Client créé avec succès",
    customerId: payload.id,
    correlationId: vars.correlationId,
    timestamp: now()
}]]></ee:set-payload>
            </ee:message>
            <ee:variables>
                <ee:set-variable variableName="customerId"><![CDATA[%dw 2.0
output application/java
---
payload.id]]></ee:set-variable>
            </ee:variables>
        </ee:transform>
        
        <flow-ref name="log-request-end-subflow" doc:name="Log Request End"/>
        
        <error-handler ref="global-api-error-handler" doc:name="Global Error Handler"/>
    </flow>

</mule>

